node_modules
release/
dist/
dist-final-verify/
dist-test/
coverage/
graphify-out/
.codex/
.kombai/
_recovery/
/data/
# Real portfolio holdings — personal financial data, never committed.
# Copy realPortfolio.example.json → realPortfolio.local.json and fill it in.
src/data/realPortfolio.local.json
*.exe
*.msi
*.db
*.db-shm
*.db-wal
*.sqlite
*.log
*.original
.vercel
.claude/worktrees/

# Gecmis replay (scripts/replay-signals.mjs) — ikisi de yeniden uretilebilir,
# depoya girmesin: onbellek ham piyasa verisi, rapor ise 1MB'lik cikti.
.replay-cache/
reports/replay-*.json

# Parametre taramasi ciktisi (yeniden uretilebilir)
reports/sweep-*.json
# KAP olay calismasi ciktisi (scripts/kap-event-study.mjs, yeniden uretilebilir)
reports/kap-event-study-*.json
# Sinyal olay calismasi ciktisi (scripts/signal-event-study.mjs, yeniden uretilebilir)
reports/signal-event-study-*.json

# Rust engine build output (v31.42)
engine-rs/target/
# pytest / Python bytecode
__pycache__/
